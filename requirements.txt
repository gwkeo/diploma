# Core environment for the Hawkes notebook
numpy
scipy
pandas
matplotlib
scikit-learn
jupyter

# Optional only: current tick builds are unstable across Python versions
# and the notebook no longer depends on tick for inference.
# dill
# pybind11
# cmake
# git+https://github.com/X-DataInitiative/tick.git@master
